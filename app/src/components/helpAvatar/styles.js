import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    padding: 3,
    backgroundColor: "#fff",
    borderRadius: 50,
    elevation: 10,
  },
  iconPosition: {
    position: "absolute",
    right: 0,
  },
});

export default styles;
